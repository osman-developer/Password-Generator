It's a script written in python in which you can generate a random password from Uppercase letter, Lowercase letter, numbers and symbols. The password length is 20 chars but you can adjust it and put your own size.
just download it and navigate to the folder using the CLI and type : ' py Password-generator ' to execute the script.